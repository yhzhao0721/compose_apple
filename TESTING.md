# 验证记录

2026-09-21，使用本机 Chrome 和 Playwright 执行浏览器测试。

通过的场景：

- 1440×1000 桌面、320×568 小屏、390×844 手机、360×740 手机、844×390 横屏、768×1024 平板；无横向溢出，三个显示尺寸均可使用。
- 模拟触屏每次点击只投放一枚；暂停后模拟时间不增长。
- 连续 40 局随机抽选，每局恰好 11 级、前十级互不重复、最后一级为苹果乐，阵容顺序变化。
- 在测试浏览器中注入碰撞物体，逐级验证 0→1 至 9→10 的十次真实物理合成，合计得分 289，最终成功提示正确。
- 危险线超时触发结束，重新开局清空分数和物体。
- 本地 file URL 离线打开；浏览器存储被拒绝仍可开始。
- 禁用 Pointer Events 后验证 Touch Events 回退：取消手势不投放，合成点击不重复投放；75% 横向位置正确映射到游戏 x=390。
- 模拟页面隐藏后暂停，返回仍等待手动继续；取消换局保留当前物体。
- JavaScript 语法检查、Git 空白检查通过；测试过程无页面 JavaScript 异常。

边界：这些是 Chromium 浏览器与事件模拟测试，不是 Safari/WebKit 或 Android/iOS 微信真机测试。微信外链访问策略、真实地址栏和安全区表现、系统中断手势及低端机性能还需在发布地址上真机验收。当前没有远程部署。
