# 验证记录

## 成功背景与界面精简

- 路线列表仅显示 1～10 的编号与头像；移除了目标卡说明句和页脚口号。
- 浏览器中通过两枚第 9 级物体真实碰撞验证最终合成会显示成功图片。
- 通过滑块键盘操作及 Canvas 背景像素验证 0%、35%、65%、100% 透明度；重新开局隐藏成功图片。
- 检查成功画面截图、390px 手机宽度无横向溢出、无页面 JavaScript 异常；既有难度规则测试通过。

## 难度调整版本：9 位角色、280 ms 冷却、2 倍物理速度

- `node tests/balance.cjs`：100 局随机阵容不重复且固定苹果乐末级；冷却在 266.7 ms 仍阻止投放、283.3 ms 可再次投放；相同下落轨迹耗时减半；最终苹果乐不再合成；出生保护期与危险线判定不被加速。
- 浏览器回归：六种屏幕尺寸、40 次开局抽选、九次逐级合成（总分 234）、结束重开、暂停恢复、触摸回退与存储不可用场景通过，无 JavaScript 异常。
- 以下为旧版 11 级规则的历史验收记录，不代表本次调整已部署线上。

2026-09-21，使用本机 Chrome 和 Playwright 执行浏览器测试。

通过的场景：

- 1440×1000 桌面、320×568 小屏、390×844 手机、360×740 手机、844×390 横屏、768×1024 平板；无横向溢出，三个显示尺寸均可使用。
- 模拟触屏每次点击只投放一枚；暂停后模拟时间不增长。
- 连续 40 局随机抽选，每局恰好 11 级、前十级互不重复、最后一级为苹果乐，阵容顺序变化。
- 在测试浏览器中注入碰撞物体，逐级验证 0→1 至 9→10 的十次真实物理合成，合计得分 289，最终成功提示正确。
- 危险线超时触发结束，重新开局清空分数和物体。
- 本地 file URL 离线打开；浏览器存储被拒绝仍可开始。
- 禁用 Pointer Events 后验证 Touch Events 回退：取消手势不投放，合成点击不重复投放；75% 横向位置正确映射到游戏 x=390。
- 模拟页面隐藏后暂停，返回仍等待手动继续；取消换局保留当前物体。
- JavaScript 语法检查、Git 空白检查通过；测试过程无页面 JavaScript 异常。

边界：这些是 Chromium 浏览器与事件模拟测试，不是 Safari/WebKit 或 Android/iOS 微信真机测试。微信外链访问策略、真实地址栏和安全区表现、系统中断手势及低端机性能还需在发布地址上真机验收。

## 正式网站验收

已发布到 https://yhzhao0721.github.io/ ，GitHub Pages 从 `main` 分支根目录部署，HTTPS 已启用。正式网站返回 HTTP 200；手机尺寸浏览器验证了开局、11 个等级、苹果乐最终等级、全部图片和脚本加载、触摸投放以及暂停恢复，无页面 JavaScript 异常。
